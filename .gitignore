
node_modules/
.next/
.swc/

# Environment files hold secrets (passwords, API keys). Only .env.example files are committed.
.env
.env.local
**/.env

# Vercel CLI project link (created by `vercel`); local to each machine.
.vercel
